apiVersion: apps/v1
kind: Deployment
metadata:
  name: op-scim
spec:
  selector:
    matchLabels:
      app: op-scim
  replicas: 1
  template:
    metadata:
      labels:
        app: op-scim
    spec:
      containers:
      - name: op-scim
        image: 1password/scim:v1.6.0
        command: ["/op-scim/op-scim"]
        livelinessProbe:
          httpGet:
            path: /ping
            port: 8080
          initialDelaySeconds: 3
          periodSeconds: 3
        ports:
        - containerPort: 8443
        - containerPort: 8080
        - containerPort: 3002
        volumeMounts:
        - name: scimsession
          mountPath: "/secret"
          readOnly: false
        envFrom:
          - configMapRef:
            name: op-scim-config
      volumes:
      - name: scimsession
        secret:
          secretName: scimsession
